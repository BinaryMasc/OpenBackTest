/**
 * @deprecated Use useMarketDataStore. This alias keeps the public Binance
 * store import working for existing integrations while the application moves
 * to provider-neutral data connections.
 */
export { useMarketDataStore as useBinanceStore } from './useMarketDataStore';
